# AI Simple Engine Musicgen Module

The module that implements the use of the `musicgen` model for the AI Simple Engine `ai-simple-engine`.

This plugin includes:

#### Model Loaders
- `MusicgenLoader`
- `TransformersMusicgenModelExecutor`


Project archived. Migrated to `aibusy`.
